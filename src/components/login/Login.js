import { TextField, Typography } from "@material-ui/core";
import React from "react";

const Login = () => {
  return (
    <form>
      <Typography>Login form here</Typography>
    </form>
  );
};

export default Login;
